import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    flex: 1,
  },
  progressBarContainer: {
    position: 'absolute',
    width: '100%',
    zIndex: 20,
  },
});
